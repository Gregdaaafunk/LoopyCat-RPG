app_identifier(ENV.fetch("APP_IDENTIFIER", "com.yourcompany.loopycat.photoprototype"))

# Optional local signing hints. Keep real values in environment variables.
team_id(ENV["APPLE_TEAM_ID"]) if ENV["APPLE_TEAM_ID"]
itc_team_id(ENV["ASC_TEAM_ID"]) if ENV["ASC_TEAM_ID"]
